# additional resources like ingress rules, cluster and repository secrets.
resources:
  - deployment.yml
  - service.yml
  - namespace.yml
  - redis.yml
  - pre-sync-job.yml
#  - cron-job.yml

namespace: guestbook

namePrefix: demo-

commonAnnotations:
  demo: argo-cd